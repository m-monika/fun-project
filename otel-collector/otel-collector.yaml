receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

exporters:
  file:
    path: /var/log/otel/traces.json
    rotation:
      max_megabytes: 20
      max_days: 3
      max_backups: 5

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [file]
